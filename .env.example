# API Configuration for Task Management Frontend
# Copy this file to .env and update the URL based on your setup

# Default local development (when backend is running locally)
VITE_API_URL=http://localhost:8080/tasks

# Docker development (when using docker-compose with mock backend)
# VITE_API_URL=http://localhost:8080/tasks

# Docker internal network (when frontend and backend both in Docker)
# VITE_API_URL=http://mock-backend:8080/tasks

# Examples for different deployment scenarios:
# NodePort (Kubernetes with NodePort service)
# VITE_API_URL=http://localhost:30080/tasks

# LoadBalancer (Kubernetes with LoadBalancer service)
# VITE_API_URL=http://your-external-ip:8080/tasks

# Ingress (Kubernetes with Ingress controller)
# VITE_API_URL=http://your-ingress-host/tasks

# Port-forward (kubectl port-forward)
# VITE_API_URL=http://localhost:8080/tasks

# Production deployment
# VITE_API_URL=https://your-production-api.com/tasks
